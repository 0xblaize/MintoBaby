import { ComingSoon } from '../components/ui';

export default function CopyMintPage() {
  return (
    <ComingSoon
      title="Copy-Mint Radar is not available yet"
      badge="Coming soon"
    >
      Whale-wallet mirroring is being rebuilt on dedicated per-network adapters with real
      block decoding, price caps, and preflight simulation. Until those adapters pass their
      safety tests, this feature stays switched off everywhere — including the bot engine —
      rather than guessing at transactions. It will appear here the moment it is genuinely wired up.
    </ComingSoon>
  );
}
